# 🚀 Инструкция по деплою на Render

## ✅ Подготовка проекта завершена!

Все необходимые файлы для деплоя созданы:
- ✓ `Procfile` - команда запуска веб-сервера
- ✓ `requirements.txt` - зависимости Python (с PostgreSQL)
- ✓ `runtime.txt` - версия Python 3.11.7
- ✓ `build.sh` - скрипт сборки
- ✓ `.gitignore` - игнорируемые файлы
- ✓ `settings.py` - настроен для продакшена с PostgreSQL

---

## 📋 Шаги для деплоя на Render:

### Шаг 1: Создайте Git репозиторий

Если еще не создан, выполните в терминале:

```bash
git init
git add .
git commit -m "Prepare for Render deployment"
```

### Шаг 2: Загрузите проект на GitHub

1. Создайте новый репозиторий на [GitHub](https://github.com/new)
2. Выполните команды:

```bash
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git branch -M main
git push -u origin main
```

---

### Шаг 3: Создайте PostgreSQL базу данных на Render

1. Перейдите на https://dashboard.render.com/
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Заполните форму:
   - **Name**: `prudok-db` (или любое имя)
   - **Database**: `prudok`
   - **User**: оставьте автоматически
   - **Region**: выберите ближайший (например, Frankfurt для Европы)
   - **PostgreSQL Version**: выберите последнюю
   - **Instance Type**: **Free**
4. Нажмите **"Create Database"**
5. **ВАЖНО!** После создания скопируйте **Internal Database URL** из раздела "Connections"
   - Выглядит примерно так: `postgresql://user:password@hostname/database`
   - Сохраните её, она понадобится в следующем шаге!

---

### Шаг 4: Создайте Web Service на Render

1. На https://dashboard.render.com/ нажмите **"New +"** → **"Web Service"**
2. Выберите **"Build and deploy from a Git repository"**
3. Подключите ваш Git репозиторий (GitHub/GitLab)
4. Заполните настройки:

   **Basic Settings:**
   - **Name**: `prudok` (или любое имя - это будет часть URL)
   - **Region**: тот же, что и для базы данных
   - **Branch**: `main`
   - **Root Directory**: оставьте пустым
   - **Runtime**: `Python 3`

   **Build & Deploy:**
   - **Build Command**: `./build.sh`
   - **Start Command**: `cd prudok && gunicorn prudok.wsgi --log-file -`

   **Instance Type:**
   - Выберите **Free**

---

### Шаг 5: Настройте переменные окружения (Environment Variables)

В разделе **"Environment"** добавьте следующие переменные:

#### Обязательные переменные:

1. **DATABASE_URL**
   - Значение: вставьте **Internal Database URL** из Шага 3
   - Пример: `postgresql://prudok_user:xxx@dpg-xxx.frankfurt-postgres.render.com/prudok`

2. **SECRET_KEY**
   - Сгенерируйте новый секретный ключ на https://djecrety.ir/
   - Пример: `django-insecure-!$%#&_very_long_random_string_here`

3. **DEBUG**
   - Значение: `False`

4. **ALLOWED_HOSTS**
   - Значение: ваш домен Render (узнаете после создания сервиса)
   - Пример: `prudok.onrender.com`
   - ⚠️ Можно добавить позже после создания!

5. **PYTHON_VERSION**
   - Значение: `3.11.7`

#### Пример заполнения:
```
DATABASE_URL = postgresql://prudok_user:xxx@dpg-xxx.frankfurt-postgres.render.com/prudok
SECRET_KEY = django-insecure-xyz123abc456def789...
DEBUG = False
ALLOWED_HOSTS = prudok.onrender.com
PYTHON_VERSION = 3.11.7
```

---

### Шаг 6: Запустите деплой

1. Нажмите **"Create Web Service"**
2. Render автоматически начнет:
   - Скачивание кода из репозитория
   - Установку зависимостей
   - Сборку статических файлов
   - Запуск миграций базы данных
   - Запуск сервера
3. Процесс займет **5-10 минут**
4. Следите за процессом в разделе **"Logs"**
5. После завершения статус изменится на **"Live"** 🟢

---

### Шаг 7: Обновите ALLOWED_HOSTS

После успешного деплоя:

1. Скопируйте ваш URL (например: `https://prudok.onrender.com`)
2. Вернитесь в **"Environment"**
3. Обновите переменную **ALLOWED_HOSTS**:
   - Значение: `prudok.onrender.com` (без https://)
4. Сохраните - Render автоматически перезапустится

---

### Шаг 8: Создайте суперпользователя Django

Чтобы войти в админ-панель:

1. В панели вашего Web Service нажмите **"Shell"** (справа в меню)
2. Выполните команды:

```bash
cd prudok
python manage.py createsuperuser
```

3. Введите:
   - Username (имя пользователя)
   - Email (можно пропустить - Enter)
   - Password (будет невидим при вводе)
   - Password (confirmation)

4. Готово! Теперь можете войти в админку: `https://ваш-сайт.onrender.com/admin`

---

## 🎉 Готово!

Ваш сайт доступен по адресу: **https://ваше-имя.onrender.com**

Админ-панель: **https://ваше-имя.onrender.com/admin**

---

## 📝 Важные замечания:

### Free план на Render:

- ⏱️ **Спящий режим**: сервис засыпает после 15 минут неактивности
- 🐌 **Холодный старт**: первый запрос после "сна" занимает 30-60 секунд
- 💾 **База данных**: PostgreSQL сохраняет все данные (в отличие от SQLite)
- 🔄 **750 часов/месяц**: бесплатно (примерно 31 день)

### Медиа-файлы:

- ⚠️ Загруженные изображения НЕ сохраняются при перезапуске
- Рекомендуется использовать внешнее хранилище:
  - **Cloudinary** (бесплатный план есть)
  - **AWS S3**
  - **Render Disks** (платно)

---

## 🔄 Обновление проекта после изменений:

После внесения изменений в код:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Render **автоматически** обнаружит изменения и запустит новый деплой!

---

## 🐛 Решение проблем:

### Сайт не открывается:

1. Проверьте **Logs** в панели Render
2. Убедитесь, что статус сервиса **"Live"** (зеленый)
3. Проверьте переменную **ALLOWED_HOSTS**

### Ошибки базы данных:

1. Проверьте **DATABASE_URL** в переменных окружения
2. Убедитесь, что PostgreSQL база создана и работает
3. Проверьте, что миграции выполнились (в логах должно быть: "Applying migrations...")

### Статические файлы не загружаются:

1. Проверьте, что в логах есть: "Collecting static files..."
2. Убедитесь, что WhiteNoise в MIDDLEWARE в settings.py

### Ошибка "Application failed to respond":

1. Проверьте **Start Command**: `cd prudok && gunicorn prudok.wsgi --log-file -`
2. Убедитесь, что **Build Command** выполнился успешно
3. Проверьте логи на ошибки Python

---

## 📚 Полезные ссылки:

- [Документация Render](https://render.com/docs)
- [Документация Django Deployment](https://docs.djangoproject.com/en/5.2/howto/deployment/)
- [Документация PostgreSQL на Render](https://render.com/docs/databases)

---

## ✅ Чек-лист перед деплоем:

- [ ] Код загружен на GitHub
- [ ] PostgreSQL база данных создана
- [ ] Web Service создан и подключен к репозиторию
- [ ] Все переменные окружения добавлены
- [ ] DATABASE_URL правильный (Internal URL)
- [ ] SECRET_KEY сгенерирован новый
- [ ] DEBUG = False
- [ ] ALLOWED_HOSTS указан правильно
- [ ] Деплой завершился успешно (статус Live)
- [ ] Суперпользователь создан
- [ ] Админ-панель открывается

Удачи с деплоем! 🚀
